'use strict';

const { BinaryTree, } = require('./tree.js');

let tree;

beforeEach(() => {
  tree = new BinaryTree();
});

describe('Testing Binary tree class', () => {

  it('testing an empty tree', () => {

  });

  it('return maxuimum value fro tree', () => {

  });

});
